@model MotorShop.ViewModels.EmailViewModel
@using MotorShop.Utilities
@{
    ViewData["Title"] = "Quản lý email";
}

<section class="min-h-screen bg-gray-50 flex items-center py-12 sm:px-6 lg:px-8">
    <div class="mx-auto w-full max-w-2xl">
        <div class="bg-white/90 backdrop-blur border border-gray-100 shadow-xl rounded-2xl p-6 sm:p-8">
            <div class="text-center">
                <div class="w-16 h-16 bg-blue-600 rounded-xl grid place-items-center mx-auto shadow-lg">
                    <i class="fa-solid fa-at text-white text-2xl"></i>
                </div>
                <h1 class="mt-4 text-3xl font-extrabold text-gray-900">@ViewData["Title"]</h1>
                <p class="mt-2 text-sm text-gray-600">Xác minh email hiện tại hoặc yêu cầu đổi sang email mới.</p>
            </div>

            <!-- TempData alerts -->
            @if (TempData[SD.Temp_Success] is string ok)
            {
                <div class="mt-5 rounded-md bg-green-50 p-4 text-green-700">@ok</div>
            }
            @if (TempData[SD.Temp_Info] is string inf)
            {
                <div class="mt-5 rounded-md bg-blue-50 p-4 text-blue-700">@inf</div>
            }
            @if (TempData[SD.Temp_Warning] is string warn)
            {
                <div class="mt-5 rounded-md bg-yellow-50 p-4 text-yellow-800">@warn</div>
            }
            @if (TempData[SD.Temp_Error] is string er)
            {
                <div class="mt-5 rounded-md bg-red-50 p-4 text-red-700">@er</div>
            }

            <!-- Email hiện tại -->
            <div class="mt-6 p-4 rounded-xl border">
                <div class="flex items-center justify-between gap-3">
                    <div>
                        <div class="text-sm text-gray-500">Email hiện tại</div>
                        <div class="text-lg font-semibold text-gray-900">@Model.Email</div>
                        @if (Model.IsEmailConfirmed)
                        {
                            <span class="inline-flex items-center gap-1 mt-1 text-xs text-green-700 bg-green-50 border border-green-200 px-2 py-0.5 rounded-full">
                                <i class="fa-solid fa-circle-check"></i> Đã xác minh
                            </span>
                        }
                        else
                        {
                            <span class="inline-flex items-center gap-1 mt-1 text-xs text-red-700 bg-red-50 border border-red-200 px-2 py-0.5 rounded-full">
                                <i class="fa-regular fa-circle-xmark"></i> Chưa xác minh
                            </span>
                        }
                    </div>

                    <div class="text-right">
                        <form asp-action="SendVerificationEmail" method="post" class="inline">
                            @Html.AntiForgeryToken()
                            <button type="submit"
                                    class="inline-flex gap-2 items-center px-4 py-2 rounded-xl border border-gray-200 hover:bg-gray-50">
                                <i class="fa-regular fa-paper-plane text-gray-700"></i> Gửi email xác minh
                            </button>
                        </form>
                    </div>
                </div>

                <p class="mt-3 text-xs text-gray-500">
                    Nếu không thấy email, kiểm tra mục <b>Spam/Junk</b> hoặc tìm từ khoá <b>MotorShop</b>.
                </p>
            </div>

            <!-- Đổi email -->
            <div class="mt-6">
                <h2 class="text-lg font-semibold text-gray-900 mb-2">Đổi sang email khác</h2>

                <form asp-action="Email" method="post" class="space-y-4" novalidate>
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="hidden"></div>
                    @if (!ViewData.ModelState.IsValid)
                    {
                        <div class="rounded-md bg-red-50 p-4 mb-2 text-sm text-red-700">
                            @foreach (var err in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                            {
                                <div>@err.ErrorMessage</div>
                            }
                        </div>
                    }

                    <div>
                        <label asp-for="NewEmail" class="block text-sm font-medium text-gray-700"></label>
                        <input asp-for="NewEmail" autocomplete="email" placeholder="new@example.com"
                               class="mt-1 block w-full rounded-xl border-gray-300 shadow-sm focus:ring-blue-500 focus:border-blue-500" />
                        <span asp-validation-for="NewEmail" class="text-sm text-red-600"></span>
                    </div>

                    <button type="submit"
                            class="w-full sm:w-auto inline-flex justify-center items-center gap-2 px-5 py-2.5 rounded-xl shadow-sm text-white bg-blue-600 hover:bg-blue-700 font-semibold">
                        <i class="fa-solid fa-link"></i> Gửi liên kết xác nhận tới email mới
                    </button>

                    <div class="text-sm text-gray-600 bg-blue-50 border border-blue-100 rounded-xl p-3">
                        <i class="fa-regular fa-circle-question mr-1 text-blue-600"></i>
                        Sau khi nhấn, chúng tôi sẽ gửi một liên kết xác nhận đến địa chỉ mới. Hãy mở liên kết để hoàn tất thay đổi.
                    </div>
                </form>
            </div>

            <div class="mt-8 text-center text-sm text-gray-500">
                <a asp-action="Index" class="text-blue-600 hover:text-blue-500 font-medium">Quay lại Hồ sơ</a>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
