@{
    ViewData["Title"] = "Xác nhận Quên Mật khẩu";
}

<div class="min-h-screen bg-gray-50 flex flex-col justify-center py-12 sm:px-6 lg:px-8">
    <div class="sm:mx-auto sm:w-full sm:max-w-md text-center">
        <div class="flex items-center justify-center h-16 w-16 mx-auto bg-green-100 rounded-full mb-4">
            <i class="fas fa-envelope-open-text text-3xl text-green-600"></i>
        </div>
        <h2 class="mt-6 text-3xl font-extrabold text-gray-900">
            Kiểm tra Email của bạn
        </h2>
        <p class="mt-4 text-md text-gray-600">
            Chúng tôi đã gửi một liên kết đến email của bạn. Vui lòng kiểm tra hộp thư đến (và thư mục spam) để đặt lại mật khẩu.
        </p>
         <div class="mt-8">
            <a asp-action="Login" class="font-medium text-blue-600 hover:text-blue-500">
                Quay lại Đăng nhập
            </a>
        </div>
    </div>
</div>